[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
# Dependencies needed for the macros
common = { path = "../common" }
processors = { path = "../processors" }
tracing = "0.1"
